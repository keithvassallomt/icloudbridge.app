version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - npm run docs:build

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
  discard-paths: no
